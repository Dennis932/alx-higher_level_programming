#include "main.h"

/**
 * main -entry point
 * Description: test the positive_or_negative function
 * Return: always 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
